// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xtimer.h"

extern XTimer_Config XTimer_ConfigTable[];

XTimer_Config *XTimer_LookupConfig(u16 DeviceId) {
	XTimer_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XTIMER_NUM_INSTANCES; Index++) {
		if (XTimer_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XTimer_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XTimer_Initialize(XTimer *InstancePtr, u16 DeviceId) {
	XTimer_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XTimer_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XTimer_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

